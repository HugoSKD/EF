import React from "react";
import ReactDOM from "react-dom/client";
import EduFinance from "./EduFinance.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <EduFinance />
  </React.StrictMode>
);
